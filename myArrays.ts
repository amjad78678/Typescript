//Arrays in typescript

const superHeros: string[] = [];
//same but diff syntax = Array<number>=[]

type User = {
  name: string;
  isActive: boolean;
};

const allUsers: User[] = [];

allUsers.push({ name: "", isActive: false });

superHeros.push("amjad");

const mlModels: number[][]=[
    [12,32,45],
    [43,54,65]
]


